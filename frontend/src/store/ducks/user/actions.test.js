import * as types from "./types"
import * as actions from "./actions"

describe('Users duck actions', () => {
    it('should execute loginRequest action', () => {

    })

    it('should execute loginResponse action', () => {
        
    })

    it('should execute logout action', () => {
        
    })

    it('should execute fetchProfileRequest action', () => {
        
    })

    it('should execute fetchProfileResponse action', () => {
        
    })

    it('should execute refreshTokenRequest action', () => {
        
    })

    it('should execute updateProfileRequest action', () => {
        
    })

    it('should execute updateProfileResponse action', () => {
        
    })
})